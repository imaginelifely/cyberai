# Frontend

This folder contains the Vite-based frontend for the HCDSF dashboard.

## Run
1. `npm install`
2. `npm run dev`

The frontend assumes the backend API is available at `http://127.0.0.1:8000`.
